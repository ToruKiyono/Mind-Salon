# Pattern Library

Pattern Library 是可复用模式清单。

## Current Library

- `pattern.plan_review_execute`
- `pattern.debate_then_execute`
- `pattern.incremental_delivery`
- `pattern.parallel_specialists`

每个条目必须引用对应的 Pattern Spec。
