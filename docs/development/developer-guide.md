# Developer Guide

本指南用于统一 Mind Salon 开发实践。

## Start Here

1. 先阅读 `system-overview/README.md`
2. 再阅读 `architecture/` 与 `runtime/`
3. 实现前确认 `core-models/` 契约
4. 协作逻辑遵循 `collaboration/`

## Development Rules

- 所有状态流转必须可追踪
- 所有消息必须结构化
- 协议门禁必须先于执行
- 关键决策必须写入日志
